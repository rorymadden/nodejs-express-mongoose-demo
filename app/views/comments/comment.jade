
if (comment && comment.user)
  .comment
    a(href="/users/"+comment.user.username) #{comment.user.name}
    |  :&nbsp;
    != comment.body
    br
    //- span.date #{formatDate(comment.createdAt, "%b %d, %Y at %I:%M %p")}
